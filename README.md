# 🚗 Vehicle Detection & Tracking System
**Created by Hem**

An AI-powered traffic monitoring system with **97% accuracy** using YOLOv8.

## 🌟 Features
* **Real-time Detection:** Detects cars, motorbikes, rickshaws, and more.
* **Persistent Tracking:** Each vehicle is assigned a unique ID that stays constant.
* **Dual Interface:** Includes a high-performance Python script and a Streamlit Web App.
* **Windows Optimized:** Custom fix included for the `lap` module error.

## 🚀 How to Run
1. Install dependencies: `pip install -r requirements.txt`
2. Run the Live Monitor: `python predict.py`
3. Run the Web App: `streamlit run app.py`